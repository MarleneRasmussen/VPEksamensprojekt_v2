package dungeonCrawlerGame.gameManager.tools;

import dungeonCrawlerGame.Config;
import dungeonCrawlerGame.locations.DungeonMap;


import java.util.List;

public class CollisionDetector {

   /* public static void checkObjectCollisions(List<GameObject> gameObjects, Player1 player){
        for (int i = 0; i < gameObjects.size(); i++) {
            if (gameObjects.get(i).getLocation() == DungeonMap.getCurrentWorldLocation()) {
                    if (gameObjects.get(i).position.getX() < player.playerPosX + Config.CELL_SIZE &&
                            gameObjects.get(i).position.getX() + gameObjects.get(i).size.getWidth() > player.playerPosX &&
                            gameObjects.get(i).position.getY() < player.playerPosY + Config.CELL_SIZE &&
                            gameObjects.get(i).position.getY() + gameObjects.get(i).size.getHeight() > player.playerPosY){
                        //handle collision between objects
                        System.out.println("Collision detected");
                    }
                }
            }
        }*/
    }

